repos:
  # Basic pre-commit hooks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: check-case-conflict
      - id: check-merge-conflict
      - id: check-toml
      - id: debug-statements
      - id: check-builtin-literals
      - id: check-docstring-first
      - id: requirements-txt-fixer

  # Python code formatting with black
  - repo: https://github.com/psf/black
    rev: 24.8.0
    hooks:
      - id: black
        language_version: python3
        args: [--line-length=120]

  # Import sorting with isort
  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        args: [--profile=black]

  # Python linting with flake8
  - repo: https://github.com/pycqa/flake8
    rev: 7.1.1
    hooks:
      - id: flake8
        args:
          - --max-line-length=120
          - --extend-ignore=E203,W503,F401,F841,E402
          - --exclude=__pycache__,.git,.tox,dist,doc,*.egg,venv,./venv

  # Security scanning with bandit (temporarily disabled for initial setup)
  # - repo: https://github.com/pycqa/bandit
  #   rev: 1.7.9
  #   hooks:
  #     - id: bandit
  #       args: [-c, pyproject.toml]
  #       exclude: ^tests/

  # Type checking with mypy (temporarily disabled for initial setup)
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.11.1
  #   hooks:
  #     - id: mypy
  #       additional_dependencies: [types-requests]
  #       args: [--ignore-missing-imports, --no-strict-optional]

  # JSON formatting
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v4.0.0-alpha.8
    hooks:
      - id: prettier
        types: [json]
        exclude: ^proposals/

  # Markdown formatting (temporarily disabled for initial setup)
  # - repo: https://github.com/igorshubovych/markdownlint-cli
  #   rev: v0.41.0
  #   hooks:
  #     - id: markdownlint
  #       args: [--fix]

  # Python docstring formatting (temporarily disabled for initial setup)
  # - repo: https://github.com/pycqa/pydocstyle
  #   rev: 6.3.0
  #   hooks:
  #     - id: pydocstyle
  #       args:
  #         - --convention=google
  #         - --add-ignore=D100,D101,D102,D103,D104,D105,D107
